name: Create Production Builds

jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: android-apk

  build_ios_simulator:
    type: build
    params:
      platform: ios
      profile: ios-simulator
